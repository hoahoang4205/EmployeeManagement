server:
  port: 9090
  servlet:
    context-path: /user-management

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/user-management-system"
    username: root
    password: 12345
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  spring:
    security:
      debug: true
